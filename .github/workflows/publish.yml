name: Publish to npm

# Fires when a maintainer publishes a GitHub Release (not on every tag push,
# so release notes get written deliberately). Requires an NPM_TOKEN repo
# secret with publish rights for @kevinprk/mycelium — add it under
# Settings → Secrets and variables → Actions before the first release.
#
# The Homebrew tap bump is a separate workflow (publish-homebrew.yml), not a
# second job here — keeping them as two independent workflow runs means a
# broken tap bump shows up as its own clearly-failed run instead of being
# folded into (and hidden inside) this workflow's own overall status.
on:
  release:
    types: [published]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # required for npm provenance

    steps:
      - name: Check out code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v7
        with:
          node-version: "22.x"
          registry-url: "https://registry.npmjs.org"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Publish
        run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
