# Canopy (가칭) — 프로젝트 개요

## 한 줄 소개

**모델이 바뀌어도, 몇 주가 지나도, 태그를 안 달아놔도 — 예전에 뭘 하고 있었는지 절대 잃어버리지 않는 AI 협업 기록 시스템.**

## 문제

AI 코딩 에이전트(Claude Code, Codex, Kiro 등)로 일하는 시간이 늘어날수록, 그 과정에서 쌓인 컨텍스트는 흩어지고 사라집니다:

- **모델을 바꾸는 순간** — 쿼터 한도, 비용, 성능 등의 이유로 다른 모델을 써야 할 때, 지금까지의 맥락을 처음부터 다시 설명해야 합니다. 그 재설명 비용이 그냥 기다리는 비용보다 크게 느껴지니, 사람들은 전환을 포기하고 기다립니다 — 기술적으로는 해결 가능한 한도 문제가 실질적인 막힘이 됩니다.
- **시간이 지나는 순간** — 몇 주 전 세션에서 무슨 결정을 내렸는지, 왜 그렇게 했는지는 기록되어 있어도 찾을 수가 없습니다. 검색창 하나로는 정확한 단어를 기억하지 못하면 무용지물입니다.
- **여러 에이전트를 넘나드는 순간** — 같은 티켓/프로젝트를 Claude Code로도, Kiro로도 다뤘다면, 그 둘을 하나로 묶어서 볼 방법이 없습니다.

세 가지 다 컨텍스트가 "사라진" 게 아닙니다. 어딘가에 그대로 있지만, 경계(모델 경계, 시간 경계, 도구 경계)를 넘는 순간 다시 꺼내 쓸 수 없게 될 뿐입니다.

## 핵심 아이디어: 세 개 층이 사실 하나의 문제

이 프로젝트는 겉보기에 다른 세 가지 기능처럼 보이지만, 실은 "컨텍스트가 어떤 경계를 넘어도 살아있게 만든다"는 하나의 논지를 세 각도에서 푸는 것입니다.

```
┌─────────────────────────────────────────────┐
│  실행 층 — 모델 경계를 넘어도 끊기지 않는다        │
│  쿼터/작업 성격에 맞게 라우팅, 컨텍스트는 그대로      │
├─────────────────────────────────────────────┤
│  조직화 층 — 시간 경계를 넘어도 찾을 수 있다        │
│  폴더처럼 중첩되는 구조 + 태그로 다시 꺼내 쓴다      │
├─────────────────────────────────────────────┤
│  인사이트 층 — 스스로 정리하고, 스스로 똑똑해진다   │
│  내용을 읽고 분류/요약하고, 그 인사이트로 에이전트   │
│  자신의 기억을 자동 업데이트한다                  │
└─────────────────────────────────────────────┘
```

**실행 층**: 태스크를 큐에 넣으면, 지금 쿼터가 남아있는 모델(Claude Code든 Codex든)에게 자동으로 배분됩니다. 단순히 "누가 쿼터가 남았나"만 보는 게 아니라, **작업의 복잡도에 맞춰 라우팅**합니다 — 간단한 작업은 가볍고 빠른 모델에, 복잡한 작업은 더 강력한 모델에 배정합니다. 이 판단은 특히 쿼터가 소진되어 남은 선택지가 제한적일 때 더 중요해집니다 — 남은 모델 중에서도 이 작업이 감당 가능한 복잡도인지를 함께 고려해서 배정합니다. 작업 도중 쿼터에 걸리면, 지금까지의 작업 상태(코드 diff)와 의도(왜 그렇게 했는지)를 담은 핸드오프를 만들어서 다른 모델이 그 자리에서 이어받습니다. 코드 산출물은 원본 그대로(git이 진실의 원천), 의도만 요약해서 넘어갑니다.

**조직화 층**: 회사/프로젝트/세션 같은 고정된 3단 개념 대신, **누구나 이미 익숙한 폴더 구조**로 세션을 정리합니다 — 폴더 안에 폴더를 만들고, 세션을 옮기고, 원하는 깊이만큼 중첩시킬 수 있습니다. 파일 탐색기를 쓰듯 직관적으로 다룰 수 있다는 게 핵심입니다. 검색으로 "찾아지는" 게 아니라, 폴더 위 자기 위치에서 상위 폴더들을 따라 올라가면 관련 컨텍스트가 자동으로 상속됩니다 — `CLAUDE.md`가 디렉토리를 따라 상속되는 것과 같은 원리입니다. 여기에 여러 태그를 동시에 붙일 수 있어서(계층형, 다중 할당), "프로젝트 X 작업이면서 동시에 긴급 티켓"처럼 한 세션이 폴더 구조와 별개로 여러 카테고리에 속하는 것도 자연스럽게 표현됩니다.

**인사이트 층**: 태그를 사람이 일일이 달지 않습니다. 세션 내용을 읽고 "이건 결제 서비스 로그인 버그다", "이건 HR 온보딩 관련이다"를 판단해서 자동으로 분류합니다. 이미 쌓인 과거 세션들에도 소급 적용됩니다. 매일, 그리고 매주 정기적으로 그 기간 있었던 일을 서사형으로 요약해서 남깁니다 — 사람이 의무감으로 확인하는 상태 리포트가 아니라, 아침에 커피 마시며 읽고 싶은 글입니다. 여기서 한 걸음 더 나아가서, 이 요약 과정에서 뽑아낸 인사이트(자주 반복되는 패턴, 이 프로젝트의 컨벤션, 자주 틀리는 지점 등)를 **에이전트가 다음 세션에서 참고할 컨텍스트/메모리에 자동으로 반영**합니다. 즉 사람이 읽는 요약을 만드는 데서 끝나지 않고, 그 인사이트가 에이전트 자신의 기억으로 흡수되어 다음 작업부터 더 나아지는 자기개선(self-improve) 루프를 만듭니다.

## 왜 이 세 층이 하나로 묶여야 하는가

각 층에 해당하는 성숙한 오픈소스가 이미 개별적으로 존재합니다 — 실행 층에 준하는 걸 하는 도구, 조직화 층에 준하는 걸 하는 도구, 인사이트 층에 준하는 걸 하는 도구가 각각 있습니다. 하지만 **세 층을 하나의 일관된 시스템으로 묶은 곳은 없습니다.** 이게 이 프로젝트의 기여입니다 — 완전히 새로운 기능 하나를 발명하는 게 아니라, 흩어진 조각들이 서로를 강화하도록 설계하는 것입니다:

- 조직화 층의 폴더 구조가 없으면, 실행 층의 핸드오프는 "지금 이 태스크"만 알 뿐 그 상위 폴더/프로젝트의 더 넓은 맥락을 모릅니다.
- 인사이트 층의 자동 분류가 없으면, 조직화 층은 사람이 계속 수동으로 정리해야 해서 결국 쓰이지 않게 됩니다.
- 실행 층이 없으면, 나머지 둘은 그냥 "과거를 잘 정리해서 보여주는" 수동적인 아카이브에 머무릅니다.
- 인사이트 층이 그 인사이트를 다시 에이전트의 메모리로 흘려보내지 않으면, 실행 층은 매번 같은 실수를 반복하는 채로 남습니다 — 인사이트 층이 실행 층을 실제로 더 낫게 만드는 연결고리입니다.

세 층이 함께 있어야 "컨텍스트가 절대 끊기지 않는다"는 약속이 실제로 성립합니다.

## 핵심 기능 (요약)

1. 여러 모델(Claude Code, Codex, 향후 Kiro 등)에 걸친 쿼터 인지형 작업 큐
2. 작업 복잡도를 고려한 라우팅 (쿼터가 남은 모델 중에서도 작업 성격에 맞는 모델 선택)
3. 모델 전환 시 코드 상태(무손실) + 의도(요약)를 함께 넘기는 핸드오프
4. 폴더처럼 자유롭게 중첩 가능한 세션 조직화 구조, 상위 폴더 경로 기반 컨텍스트 상속
5. 세션에 다중/계층형 태그 부여 및 태그+검색 결합 필터링
6. 세션 내용을 이해해서 자동으로 분류하는 태깅 (디렉토리 이름 패턴이 아니라 실제 내용 기반), 과거 세션에도 소급 적용
7. 정기적으로 생성되는 서사형 일일/주간 요약
8. 요약 과정에서 뽑아낸 인사이트를 에이전트의 컨텍스트/메모리에 자동 반영하는 자기개선 루프

## 현재 상태

이 문서는 개념 설계 단계입니다. 세 층 중 **실행 층은 이미 별도 프로젝트(Relay)로 만들어져 실제 Claude Code/Codex 태스크로 검증까지 마쳤고** (복잡도 기반 라우팅은 아직 미구현), 이 프로젝트는 그 위에 조직화 층과 인사이트 층을 얹어 하나의 완결된 시스템으로 만드는 것을 목표로 합니다. 조직화/인사이트 층은 아직 설계만 되어 있고 구현은 시작 전입니다.
