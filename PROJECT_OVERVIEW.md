# Mycelium (가칭)

## 한 줄 소개

> **AI에서 생성되는 컨텍스트를 생성부터 조직화, 학습, 재사용까지 관리하는 Context Lifecycle 플랫폼**

**미션**: Mycelium manages the lifecycle of AI collaboration context — from capture, to organization, to continuous learning — so context survives model switches, time, and team growth.

---

## 프로젝트가 해결하려는 문제

AI 에이전트는 이제 코딩을 넘어 개발 전반, 문서 작성, 인사(HR) 업무까지 일상적인 업무 전 영역에서 쓰이고 있습니다. 하지만 그 과정에서 생성되는 **컨텍스트(Context)** 는 여전히 쉽게 사라집니다. 정확히 말하면 사라지는 것이 아니라 **활용할 수 없는 상태**가 됩니다. 핵심 페인 포인트는 세 가지입니다.

1. **컨텍스트 핸드오프의 부재 (모델 경계)** — Claude Code의 사용량(Quota)이 소진되어 Codex로 전환해야 하는 순간, 지금까지의 맥락을 처음부터 다시 설명해야 합니다. 그 재설명 비용이 대기 비용보다 크게 느껴지기 때문에, 기술적으로 해결 가능한 한도 문제가 실질적인 작업 중단이 됩니다.
2. **컨텍스트 탐색/조직화의 부재 (시간·공간 경계)** — 기술 티켓 처리, 프로젝트 작업, 인사 관련 업무처럼 성격이 다른 세션들이 하나의 목록에 뒤섞여 쌓입니다. 몇 주 전 다뤘던 티켓이나 당시의 의사결정을 다시 찾으려 해도, 검색창 하나로는 정확한 단어를 기억하지 못하면 무용지물입니다. 여러 에이전트(Claude Code, Codex, Kiro 등)와 팀원이 함께 작업하면 세션은 더 넓게 흩어집니다.
3. **인사이트 환류의 부재** — 컨텍스트가 아무리 쌓여도, 거기서 얻을 수 있는 인사이트(반복되는 패턴, 컨벤션, 자주 하는 실수)는 묻힌 채로 남습니다. AI는 매번 같은 것을 다시 배우고, 같은 실수를 반복합니다.

현재 대부분의 AI 도구는 **세션(Session)** 단위를 관리할 뿐, 업무 전체의 컨텍스트를 장기적으로 관리하지 않습니다. Mycelium은 이 문제를 **Context Lifecycle** 관점에서 해결합니다.

---

## 핵심 아이디어: Context Lifecycle

Mycelium은 AI 협업 과정에서 생성되는 컨텍스트를 일회성 대화가 아니라 **계속 성장하는 프로젝트 자산**으로 취급합니다. 컨텍스트는 다음 생명주기를 순환합니다.

```
생성(Capture) → 조직화(Organize) → 학습(Learn) → 재사용(Reuse)
      ↑                                              │
      └──────────── 다음 작업에서 다시 생성 ←──────────┘
```

오늘 생성된 컨텍스트가 내일의 AI 작업을 더 똑똑하게 만들고, 프로젝트가 진행될수록 AI도 함께 프로젝트를 이해하게 됩니다.

---

## 시스템 구조

### 1. Context Capture — 생성

사용자가 AI와 협업하는 과정에서 다양한 컨텍스트가 자연스럽게 생성됩니다: AI와의 대화, 코드 변경(Diff), 문서 초안, 의사결정(Decision), TODO, 작업 이력. Mycelium은 이를 자동으로 수집합니다 — 사람이 수동으로 정리해야 한다면 급한 순간에는 결국 하지 않게 되기 때문에, **자동 포착이 원칙**입니다.

특히 이 단계에서 컨텍스트는 **특정 모델의 세션 포맷에 종속되지 않는 형태**로 기록됩니다. 산출물(코드, 문서 등)은 원본 그대로 무손실로, 의도/결정은 어떤 에이전트도 소비할 수 있는 중립적 형태로 남깁니다 — 이것이 모델 간 핸드오프를 가능하게 하는 토대입니다.

이 단계의 결과는 **Raw Context**입니다.

### 2. Context Organization — 조직화

Raw Context는 시간이 지날수록 쌓이지만, 단순히 저장만 하면 "더 큰 로그 더미"일 뿐입니다. Mycelium은 이를 다음과 같이 조직화합니다:

- **폴더 기반 계층 구조** — 누구나 익숙한 파일 탐색기 방식으로, 원하는 깊이만큼 자유롭게 중첩
- **Context 상속** — 하위 폴더의 작업은 상위 폴더의 컨텍스트를 자동으로 상속 (`CLAUDE.md`가 디렉토리를 따라 상속되는 것과 같은 원리)
- **다중/계층형 태그** — 폴더 구조와 별개로, 한 세션이 "프로젝트 X 작업이면서 동시에 긴급 티켓"처럼 여러 분류에 속하는 것을 표현
- **검색 가능한 메타데이터** — 태그 필터와 텍스트 검색을 결합해 좁혀 들어가는 탐색

예를 들어 `회사 → 플랫폼 → 인증 → JWT` 구조에서 JWT 작업을 열면 상위 폴더들의 컨텍스트가 자동으로 함께 제공되고, `회사 → 인사 → 채용` 아래의 작업은 개발 컨텍스트와 섞이지 않고 인사 업무의 맥락만 상속받습니다. 관련성이 검색으로 "찾아지는" 것이 아니라 구조로 "정의되는" 것 — 이것이 검색 기반 접근과의 핵심 차이입니다. 검색이 확률적이라면, 경로 상속은 결정론적입니다.

이 단계의 결과는 **Structured Context**입니다.

### 3. Context Learning — 학습

조직화된 컨텍스트는 다시 분석되어, 다음이 자동으로 추출됩니다:

- 반복되는 설계·업무 패턴, 프로젝트 컨벤션, 조직/프로젝트 용어
- 자주 발생하는 실수, 자주 수정되는 코드/문서
- 주요 의사결정과 그 이유

또한 **일일 요약**과 **주간 요약**을 자동 생성합니다 — 상태 카운트("3개 완료, 1개 실패")가 아니라, 동료가 남긴 인수인계 메모처럼 읽히는 서사형 요약입니다.

여기서 끝나지 않습니다. 추출된 인사이트는 **AI의 메모리(Context)에 자동으로 반영**되어, 다음 작업부터 AI가 프로젝트를 더 잘 이해하게 만듭니다:

```
Context → Knowledge → 더 좋은 Context
```

사람이 읽는 요약을 만드는 데서 멈추지 않고, 그 인사이트가 에이전트 자신의 기억으로 흡수되는 **자기개선(Self-improving) 구조**입니다.

이 단계의 결과는 **Project Knowledge**입니다.

### 4. Context Reuse — 재사용

새로운 AI 작업이 시작되면, Mycelium은 프로젝트의 Knowledge를 기반으로 필요한 컨텍스트를 자동으로 제공합니다. 사용자는 더 이상 *"지난번에는 Redis를 제거하기로 했고…"* 를 다시 설명하지 않아도 됩니다.

모델이 중간에 바뀌어도 마찬가지입니다 — 쿼터 소진으로 Claude에서 Codex로 전환되는 순간, 지금까지의 작업 상태(코드 diff, 무손실)와 의도(요약)가 핸드오프로 전달되어 새 모델이 그 자리에서 이어받습니다.

---

## 전체 흐름

```
        AI Agent (Claude Code / Codex / Kiro / ...)
                        │
                        ▼
              ① Context Capture
                        │
                        ▼
                  Raw Context
                        │
                        ▼
              ② Context Organization
                        │
                        ▼
               Structured Context
                        │
                        ▼
              ③ Context Learning
                        │
                        ▼
               Project Knowledge
                        │
                        ▼
              ④ Future AI Sessions ──→ 새로운 Context 생성 ──→ ①로 순환
```

---

## 기존 오픈소스와의 차별점

이 영역에는 이미 여러 성숙한 프로젝트가 있으며, 설계 전에 직접 조사·검증했습니다. 각 프로젝트는 라이프사이클의 **일부 단계**를 다루지만, 전체 순환을 다루는 곳은 확인된 범위에서 없습니다. 또한 기존 도구들은 대부분 **코딩 에이전트 세션에 특화**되어 있는 반면, Mycelium은 개발을 포함한 일반 AI 업무 전반의 컨텍스트를 대상으로 합니다:

|  | 세션 캡처 | 계층 조직화 | 자동 정기 요약 | 인사이트→에이전트 메모리 반영 |
| --- | --- | --- | --- | --- |
| ai-memory | ✅ 훅 기반 자동 | △ 2단계(workspace/project) 고정 | ❌ 온디맨드만 | △ 위키 갱신 수준 |
| pi-session-manager | ✅ 다중 CLI 인덱싱 | ✅ 트리/태그 (수동) | ❌ | ❌ |
| memory-journal-mcp | ✅ MCP 기반 | ❌ 평면 구조 | △ HTTP 모드 전용 | ❌ |
| agentmemory 계열 | ✅ | △ 프로젝트 단위 | ❌ | △ 고정 슬롯 갱신 |
| **Mycelium** | ✅ | ✅ 자유 중첩 폴더 + 상속 | ✅ 일일/주간 서사형 | ✅ 자기개선 루프 |

Mycelium의 기여는 완전히 새로운 기능 하나의 발명이 아니라, **흩어진 조각들이 서로를 강화하도록 라이프사이클 전체를 하나의 일관된 시스템으로 묶는 설계**입니다:

- 조직화가 없으면, 핸드오프는 "지금 이 태스크"만 알 뿐 상위 프로젝트의 맥락을 모릅니다.
- 자동 분류/학습이 없으면, 조직화는 사람이 계속 수동으로 해야 해서 결국 쓰이지 않게 됩니다.
- 학습된 인사이트가 에이전트 메모리로 돌아가지 않으면, AI는 매번 같은 실수를 반복합니다.

---

## 왜 Mycelium인가?

균사체(Mycelium)는 균사의 네트워크입니다. 각 균은 독립적으로 보이지만, 실제로는 균사체를 통해 영양분, 정보, 위험 신호를 공유합니다.

AI Agent도 마찬가지입니다. Claude Code, Codex, Kiro는 서로 독립적인 에이전트이지만, Mycelium은 그 사이에서 **Context와 Knowledge를 전달하는 보이지 않는 네트워크**가 됩니다.

```
   Claude Code      Codex       Kiro
        │             │           │
        └─────────────┼───────────┘
                      ▼
        ─────────────────────────────
                  Mycelium
           Context · Knowledge · Insight
        ─────────────────────────────
```

Mycelium은 AI Agent가 아니라, **AI 협업의 Context Network**입니다.

---

# 오픈소스 개발자대회 제출 양식

### 프로젝트명

**Mycelium**

### 개발 목적 (200자 이내, 현재 약 190자)

개발부터 문서 작성, 인사 업무까지 AI 활용이 업무 전 영역으로 확장되면서 컨텍스트 단절 문제가 커지고 있습니다. Mycelium은 AI 협업에서 생성되는 컨텍스트를 수집·조직화·학습·재사용하는 Context Lifecycle 플랫폼을 개발하여, 에이전트 간 컨텍스트 핸드오프, 축적된 컨텍스트의 탐색·조직화, 인사이트 기반 자기개선을 지원하는 것을 목표로 합니다.

### 프로젝트 소개 (300자 이내, 현재 약 290자)

Mycelium은 코딩·개발뿐 아니라 문서 작성, 인사 업무 등 다양한 AI 협업 과정에서 생성되는 컨텍스트를 자동으로 수집(Capture)하고, 폴더 기반 계층 구조와 태그로 조직화(Organize)하며, 반복되는 패턴과 의사결정을 학습(Learn)하여 이후 AI 작업에 다시 활용(Reuse)하는 Context Lifecycle 플랫폼입니다. 컨텍스트를 특정 모델에 종속되지 않는 중립적 형태로 관리해 에이전트 간 핸드오프를 지원하고, 작업이 쌓일수록 AI가 사용자의 업무 맥락과 컨벤션을 함께 학습하는 지속 가능한 AI 협업 환경을 제공합니다.

### 기대 효과 (500자 이내, 현재 약 470자)

AI를 활용하는 사용자는 모델 전환이나 사용량 제한으로 작업이 중단되거나 동일한 맥락을 반복 설명해야 하는 부담을 줄일 수 있고, 기술 티켓 처리·프로젝트 작업·인사 업무처럼 성격이 다른 세션이 뒤섞인 기록 속에서도 원하는 컨텍스트를 구조적으로 찾을 수 있습니다. 여러 에이전트를 함께 사용하는 환경에서도 흩어진 세션이 하나의 업무 지식으로 통합되며, 새로운 팀원이 합류할 때 축적된 컨텍스트를 통해 온보딩 비용을 크게 낮출 수 있습니다. 또한 축적된 컨텍스트에서 반복되는 패턴과 컨벤션을 자동으로 추출해 AI의 메모리에 반영함으로써, 작업이 쌓일수록 AI가 사용자의 업무를 더 잘 이해하는 자기개선(Self-improving) 구조를 제공합니다. 특정 벤더에 종속되지 않는 오픈소스로 공개되어 Claude Code, Codex, Kiro 등 다양한 에이전트 환경에서 활용될 수 있으며, 코딩을 넘어 업무 전반에서 AI 협업의 연속성과 생산성을 높이는 공통 인프라로 발전할 수 있습니다.
