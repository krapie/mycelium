# Canopy (가칭) — 프로젝트 개요

## 한 줄 소개

**모델이 바뀌어도, 몇 주가 지나도, 태그를 안 달아놔도 — 예전에 뭘 하고 있었는지 절대 잃어버리지 않는 AI 협업 기록 시스템.**

## 문제

AI 코딩 에이전트(Claude Code, Codex, Kiro 등)로 일하는 시간이 늘어날수록, 그 과정에서 쌓인 컨텍스트는 흩어지고 사라집니다:

- **모델을 바꾸는 순간** — 쿼터 한도, 비용, 성능 등의 이유로 다른 모델을 써야 할 때, 지금까지의 맥락을 처음부터 다시 설명해야 합니다. 그 재설명 비용이 그냥 기다리는 비용보다 크게 느껴지니, 사람들은 전환을 포기하고 기다립니다 — 기술적으로는 해결 가능한 한도 문제가 실질적인 막힘이 됩니다.
- **시간이 지나는 순간** — 몇 주 전 세션에서 무슨 결정을 내렸는지, 왜 그렇게 했는지는 기록되어 있어도 찾을 수가 없습니다. 검색창 하나로는 정확한 단어를 기억하지 못하면 무용지물입니다.
- **여러 에이전트를 넘나드는 순간** — 같은 티켓/프로젝트를 Claude Code로도, Kiro로도 다뤘다면, 그 둘을 하나로 묶어서 볼 방법이 없습니다.

세 가지 다 컨텍스트가 "사라진" 게 아닙니다. 어딘가에 그대로 있지만, 경계(모델 경계, 시간 경계, 도구 경계)를 넘는 순간 다시 꺼내 쓸 수 없게 될 뿐입니다.

## 핵심 아이디어: 세 개 층이 사실 하나의 문제

이 프로젝트는 겉보기에 다른 세 가지 기능처럼 보이지만, 실은 "컨텍스트가 어떤 경계를 넘어도 살아있게 만든다"는 하나의 논지를 세 각도에서 푸는 것입니다.

```
┌─────────────────────────────────────────────┐
│  실행 층 — 모델 경계를 넘어도 끊기지 않는다        │
│  쿼터가 떨어지면 다른 모델로, 컨텍스트는 그대로      │
├─────────────────────────────────────────────┤
│  조직화 층 — 시간 경계를 넘어도 찾을 수 있다        │
│  회사/프로젝트/세션 트리 + 태그로 다시 꺼내 쓴다     │
├─────────────────────────────────────────────┤
│  이해 층 — 사람이 정리 안 해도 스스로 정리된다      │
│  세션 내용을 읽고 자동으로 분류하고 요약한다         │
└─────────────────────────────────────────────┘
```

**실행 층**: 태스크를 큐에 넣으면, 지금 쿼터가 남아있는 모델(Claude Code든 Codex든)에게 자동으로 배분됩니다. 작업 도중 쿼터에 걸리면, 지금까지의 작업 상태(코드 diff)와 의도(왜 그렇게 했는지)를 담은 핸드오프를 만들어서 다른 모델이 그 자리에서 이어받습니다. 코드 산출물은 원본 그대로(git이 진실의 원천), 의도만 요약해서 넘어갑니다.

**조직화 층**: 모든 세션은 회사/클라이언트 → 프로젝트 → 세션이라는 트리 위의 한 노드입니다. 검색으로 "찾아지는" 게 아니라, 트리 위 자기 위치의 조상 경로를 따라가면 관련 컨텍스트가 자동으로 상속됩니다 — `CLAUDE.md`가 디렉토리를 따라 상속되는 것과 같은 원리입니다. 여기에 여러 태그를 동시에 붙일 수 있어서(계층형, 다중 할당), "프로젝트 X 작업이면서 동시에 긴급 티켓"처럼 한 세션이 여러 카테고리에 속하는 것도 자연스럽게 표현됩니다.

**이해 층**: 태그를 사람이 일일이 달지 않습니다. 세션 내용을 읽고 "이건 결제 서비스 로그인 버그다", "이건 HR 온보딩 관련이다"를 판단해서 자동으로 분류합니다. 이미 쌓인 과거 세션들에도 소급 적용되고, 매일/매주 정기적으로 그날 있었던 일을 서사형으로 요약해서 남깁니다 — 사람이 의무감으로 확인하는 상태 리포트가 아니라, 아침에 커피 마시며 읽고 싶은 글입니다.

## 왜 이 세 층이 하나로 묶여야 하는가

각 층에 해당하는 성숙한 오픈소스가 이미 개별적으로 존재합니다 — 실행 층에 준하는 걸 하는 도구, 조직화 층에 준하는 걸 하는 도구, 이해 층에 준하는 걸 하는 도구가 각각 있습니다. 하지만 **세 층을 하나의 일관된 시스템으로 묶은 곳은 없습니다.** 이게 이 프로젝트의 기여입니다 — 완전히 새로운 기능 하나를 발명하는 게 아니라, 흩어진 조각들이 서로를 강화하도록 설계하는 것입니다:

- 조직화 층의 트리 구조가 없으면, 실행 층의 핸드오프는 "지금 이 태스크"만 알 뿐 그 프로젝트/조직의 더 넓은 맥락을 모릅니다.
- 이해 층의 자동 분류가 없으면, 조직화 층은 사람이 계속 수동으로 정리해야 해서 결국 쓰이지 않게 됩니다.
- 실행 층이 없으면, 나머지 둘은 그냥 "과거를 잘 정리해서 보여주는" 수동적인 아카이브에 머무릅니다.

세 층이 함께 있어야 "컨텍스트가 절대 끊기지 않는다"는 약속이 실제로 성립합니다.

## 핵심 기능 (요약)

1. 여러 모델(Claude Code, Codex, 향후 Kiro 등)에 걸친 쿼터 인지형 작업 큐
2. 모델 전환 시 코드 상태(무손실) + 의도(요약)를 함께 넘기는 핸드오프
3. 회사 → 프로젝트 → 세션 트리, 조상 경로 기반 컨텍스트 상속
4. 세션에 다중/계층형 태그 부여 및 태그+검색 결합 필터링
5. 세션 내용을 이해해서 자동으로 분류하는 태깅 (디렉토리 이름 패턴이 아니라 실제 내용 기반)
6. 이미 쌓인 과거 세션에도 소급 적용되는 자동 분류
7. 정기적으로 생성되는 서사형 일일/주간 요약

## 현재 상태

이 문서는 개념 설계 단계입니다. 세 층 중 **실행 층은 이미 별도 프로젝트(Relay)로 만들어져 실제 Claude Code/Codex 태스크로 검증까지 마쳤고**, 이 프로젝트는 그 위에 조직화 층과 이해 층을 얹어 하나의 완결된 시스템으로 만드는 것을 목표로 합니다. 조직화/이해 층은 아직 설계만 되어 있고 구현은 시작 전입니다.
