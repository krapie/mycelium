# Mycelium — AI 협업 컨텍스트 라이프사이클 플랫폼

## 한 줄 소개

> **AI 협업에서 생성되는 컨텍스트를 생성부터 조직화, 학습, 재사용까지 관리하는 Context Lifecycle 플랫폼**

**미션**: Mycelium manages the lifecycle of AI collaboration context — from capture, to organization, to continuous learning — so context survives model switches, time, and team growth.

---

## 프로젝트가 해결하려는 문제

AI 코딩 에이전트는 개발 생산성을 크게 높였지만, 그 과정에서 생성되는 **컨텍스트(Context)** 는 여전히 쉽게 사라집니다. 정확히 말하면 사라지는 것이 아니라, 세 가지 경계를 넘는 순간 **활용할 수 없는 상태**가 됩니다.

1. **모델 경계** — Claude Code의 사용량(Quota)이 소진되어 Codex로 전환해야 하는 순간, 지금까지의 맥락을 처음부터 다시 설명해야 합니다. 그 재설명 비용이 대기 비용보다 크게 느껴지기 때문에, 기술적으로 해결 가능한 한도 문제가 실질적인 작업 중단이 됩니다.
2. **시간 경계** — 몇 주 전 작업했던 프로젝트를 다시 열었을 때, 당시의 의사결정과 그 이유는 기록 어딘가에 있지만 찾을 수 없습니다. 검색창 하나로는 정확한 단어를 기억하지 못하면 무용지물입니다.
3. **팀/규모 경계** — 같은 프로젝트를 여러 에이전트(Claude Code, Codex, Kiro 등)와 작업하거나 새 팀원이 합류했을 때, 흩어진 세션들을 하나의 프로젝트 지식으로 모아 볼 방법이 없습니다.

현재 대부분의 AI 도구는 **세션(Session)** 단위를 관리할 뿐, 프로젝트 전체의 컨텍스트를 장기적으로 관리하지 않습니다. Mycelium은 이 문제를 **Context Lifecycle** 관점에서 해결합니다.

---

## 핵심 아이디어: Context Lifecycle

Mycelium은 AI 협업 과정에서 생성되는 컨텍스트를 일회성 대화가 아니라 **계속 성장하는 프로젝트 자산**으로 취급합니다. 컨텍스트는 다음 생명주기를 순환합니다.

```
생성(Capture) → 조직화(Organize) → 학습(Learn) → 재사용(Reuse)
      ↑                                              │
      └──────────── 다음 작업에서 다시 생성 ←──────────┘
```

오늘 생성된 컨텍스트가 내일의 AI 작업을 더 똑똑하게 만들고, 프로젝트가 진행될수록 AI도 함께 프로젝트를 이해하게 됩니다.

---

## 시스템 구조

### 1. Context Capture — 생성

개발자가 AI와 협업하는 과정에서 다양한 컨텍스트가 자연스럽게 생성됩니다: AI와의 대화, 코드 변경(Diff), 의사결정(Decision), TODO, Git 상태, 작업 이력. Mycelium은 이를 자동으로 수집합니다 — 사람이 수동으로 정리해야 한다면 급한 순간에는 결국 하지 않게 되기 때문에, **자동 포착이 원칙**입니다.

특히 이 단계에서 컨텍스트는 **특정 모델의 세션 포맷에 종속되지 않는 형태**로 기록됩니다. 코드 산출물은 원본 그대로(git이 진실의 원천, 무손실), 의도/결정은 어떤 에이전트도 소비할 수 있는 중립적 형태로 남깁니다 — 이것이 모델 간 핸드오프를 가능하게 하는 토대입니다.

이 단계의 결과는 **Raw Context**입니다.

### 2. Context Organization — 조직화

Raw Context는 시간이 지날수록 쌓이지만, 단순히 저장만 하면 "더 큰 로그 더미"일 뿐입니다. Mycelium은 이를 다음과 같이 조직화합니다:

- **폴더 기반 계층 구조** — 누구나 익숙한 파일 탐색기 방식으로, 원하는 깊이만큼 자유롭게 중첩
- **Context 상속** — 하위 폴더의 작업은 상위 폴더의 컨텍스트를 자동으로 상속 (`CLAUDE.md`가 디렉토리를 따라 상속되는 것과 같은 원리)
- **다중/계층형 태그** — 폴더 구조와 별개로, 한 세션이 "프로젝트 X 작업이면서 동시에 긴급 티켓"처럼 여러 분류에 속하는 것을 표현
- **검색 가능한 메타데이터** — 태그 필터와 텍스트 검색을 결합해 좁혀 들어가는 탐색

예를 들어 `회사 → 플랫폼 → 인증 → JWT` 구조에서 JWT 작업을 열면, 상위 폴더들의 컨텍스트가 자동으로 함께 제공됩니다. 관련성이 검색으로 "찾아지는" 것이 아니라 구조로 "정의되는" 것 — 이것이 검색 기반 접근과의 핵심 차이입니다. 검색이 확률적이라면, 경로 상속은 결정론적입니다.

이 단계의 결과는 **Structured Context**입니다.

### 3. Context Learning — 학습

조직화된 컨텍스트는 다시 분석되어, 다음이 자동으로 추출됩니다:

- 반복되는 설계 패턴, 프로젝트 컨벤션, 프로젝트 용어
- 자주 발생하는 실수, 자주 수정되는 코드
- 아키텍처 의사결정과 그 이유

또한 **일일 요약**과 **주간 요약**을 자동 생성합니다 — 상태 카운트("3개 완료, 1개 실패")가 아니라, 동료가 남긴 인수인계 메모처럼 읽히는 서사형 요약입니다.

여기서 끝나지 않습니다. 추출된 인사이트는 **AI의 메모리(Context)에 자동으로 반영**되어, 다음 작업부터 AI가 프로젝트를 더 잘 이해하게 만듭니다:

```
Context → Knowledge → 더 좋은 Context
```

사람이 읽는 요약을 만드는 데서 멈추지 않고, 그 인사이트가 에이전트 자신의 기억으로 흡수되는 **자기개선(Self-improving) 구조**입니다.

이 단계의 결과는 **Project Knowledge**입니다.

### 4. Context Reuse — 재사용

새로운 AI 작업이 시작되면, Mycelium은 프로젝트의 Knowledge를 기반으로 필요한 컨텍스트를 자동으로 제공합니다. 사용자는 더 이상 *"지난번에는 Redis를 제거하기로 했고..."* 를 다시 설명하지 않아도 됩니다.

모델이 중간에 바뀌어도 마찬가지입니다 — 쿼터 소진으로 Claude에서 Codex로 전환되는 순간, 지금까지의 작업 상태(코드 diff, 무손실)와 의도(요약)가 핸드오프로 전달되어 새 모델이 그 자리에서 이어받습니다.

---

## 전체 흐름

```
        AI Agent (Claude Code / Codex / Kiro / ...)
                        │
                        ▼
              ① Context Capture
                        │
                        ▼
              Raw Development Context
                        │
                        ▼
              ② Context Organization
                        │
                        ▼
               Structured Context
                        │
                        ▼
              ③ Context Learning
                        │
                        ▼
               Project Knowledge
                        │
                        ▼
              ④ Future AI Sessions ──→ 새로운 Context 생성 ──→ ①로 순환
```

---

## 실행 계층과의 관계 (Relay)

Mycelium은 실행 엔진이 아닙니다 — 실행은 별도 계층이 담당하고, Mycelium은 그 실행이 어디서 일어나든(자동화된 태스크 큐를 통해서든, 사람이 직접 터미널에서 에이전트를 켜서든) 그 결과로 생성되는 컨텍스트의 생애주기를 관리합니다.

실행 계층으로는 같은 문제의식에서 출발해 먼저 개발·검증된 **Relay**를 사용합니다:

- 여러 모델(Claude Code, Codex)에 걸친 쿼터 인지형 작업 큐 — 잠들기 전 태스크를 등록하면 밤새 쿼터가 남은 모델에게 자동 분배
- 작업 도중 쿼터 소진 시, 코드 상태 + 의도를 담은 핸드오프를 만들어 다른 모델이 같은 자리에서 이어받는 크로스 벤더 전환
- git worktree 격리, 테스트 게이트, 자동 PR 생성
- 실제 Claude Code/Codex 태스크와 실제 GitHub PR로 동작 검증 완료

Relay가 "작업이 끊기지 않게" 한다면, Mycelium은 "그 과정에서 생긴 지식이 사라지지 않게" 합니다.

---

## 기존 오픈소스와의 차별점

이 영역에는 이미 여러 성숙한 프로젝트가 있으며, 설계 전에 직접 조사·검증했습니다. 각 프로젝트는 라이프사이클의 **일부 단계**를 다루지만, 전체 순환을 다루는 곳은 확인된 범위에서 없습니다:

| | 세션 캡처 | 계층 조직화 | 자동 정기 요약 | 인사이트→에이전트 메모리 반영 | 실행 계층 통합 |
|---|---|---|---|---|---|
| ai-memory | ✅ 훅 기반 자동 | △ 2단계(workspace/project) 고정 | ❌ 온디맨드만 | △ 위키 갱신 수준 | ❌ |
| pi-session-manager | ✅ 다중 CLI 인덱싱 | ✅ 트리/태그 (수동) | ❌ | ❌ | ❌ |
| memory-journal-mcp | ✅ MCP 기반 | ❌ 평면 구조 | △ HTTP 모드 전용 | ❌ | ❌ |
| agentmemory 계열 | ✅ | △ 프로젝트 단위 | ❌ | △ 고정 슬롯 갱신 | ❌ |
| **Mycelium** | ✅ | ✅ 자유 중첩 폴더 + 상속 | ✅ 일일/주간 서사형 | ✅ 자기개선 루프 | ✅ Relay |

Mycelium의 기여는 완전히 새로운 기능 하나의 발명이 아니라, **흩어진 조각들이 서로를 강화하도록 라이프사이클 전체를 하나의 일관된 시스템으로 묶는 설계**입니다:

- 조직화가 없으면, 핸드오프는 "지금 이 태스크"만 알 뿐 상위 프로젝트의 맥락을 모릅니다.
- 자동 분류/학습이 없으면, 조직화는 사람이 계속 수동으로 해야 해서 결국 쓰이지 않게 됩니다.
- 학습된 인사이트가 에이전트 메모리로 돌아가지 않으면, AI는 매번 같은 실수를 반복합니다.
- 실행 계층이 없으면, 나머지는 "과거를 잘 보여주는" 수동적 아카이브에 머무릅니다.

---

## 왜 Mycelium인가?

균사체(Mycelium)는 숲속에서 나무들을 연결하는 보이지 않는 네트워크입니다. 각 나무는 독립적으로 보이지만, 실제로는 균사체를 통해 영양분, 정보, 위험 신호를 공유합니다.

AI Agent도 마찬가지입니다. Claude Code, Codex, Kiro는 서로 독립적인 에이전트이지만, Mycelium은 그 사이에서 **Context와 Knowledge를 전달하는 보이지 않는 네트워크**가 됩니다.

```
   Claude Code      Codex       Kiro
        │             │           │
        └─────────────┼───────────┘
                      ▼
        ─────────────────────────────
                  Mycelium
           Context · Knowledge · Insight
        ─────────────────────────────
```

Mycelium은 AI Agent가 아니라, **AI 협업의 Context Network**입니다.

---

## 현재 상태와 개발 계획

| 구성 요소 | 상태 |
|---|---|
| 실행 계층 (Relay) | ✅ 개발 및 실사용 검증 완료 (실제 Claude Code/Codex 태스크, 실제 GitHub PR, 크래시 복구까지 검증) |
| Capture — 세션/컨텍스트 자동 수집 | 설계 완료, 구현 예정 |
| Organization — 폴더/태그/상속 | 설계 완료, 구현 예정 |
| Learning — 자동 분류, 일일/주간 요약, 메모리 반영 | 설계 완료, 구현 예정 |
| Reuse — 세션 시작 시 컨텍스트 자동 주입 | 설계 완료, 구현 예정 |

**개발 단계**:
1. Capture: Claude Code/Codex(/Kiro) 세션 로그 어댑터 + Relay 이벤트 수집
2. Organization: 폴더 구조 + 태그 + 조상 경로 상속
3. Learning: 내용 기반 자동 태깅(과거 세션 소급 적용 포함), 일일/주간 서사 요약
4. Reuse: 세션 시작 시 컨텍스트 자동 주입 + 인사이트의 에이전트 메모리 반영 루프
5. 웹 UI (탐색/타임라인/검색)

**기술 원칙** (Relay에서 검증된 원칙 유지):
- 외부 의존성 최소화(zero-dependency 지향) — 감사 가능하고, 빠르게 변하는 생태계에서 부서질 표면적을 최소화
- 각 벤더 공식 CLI의 "일반적인 스크립트화된 사용" 범위 준수 — OAuth 토큰 추출/재사용 등 ToS 위반 소지가 있는 방식 배제
- 저장 포맷은 사람이 읽을 수 있는 형태(markdown/JSON) 우선 — 도구가 사라져도 데이터는 남도록

**라이선스**: MIT. 의존성을 최소화하는 설계 덕분에 라이선스 충돌 위험이 구조적으로 낮습니다.

---

# 오픈소스 개발자대회 제출 양식

### 프로젝트명

**Mycelium**

### 개발 목적 (200자 이내, 현재 약 175자)

AI 코딩 에이전트 사용이 증가하면서 모델 전환, 시간 경과, 프로젝트 확장에 따라 개발 컨텍스트가 단절되는 문제가 발생한다. Mycelium은 AI 협업 과정에서 생성되는 컨텍스트를 수집·조직화·학습·재사용하는 Context Lifecycle 플랫폼을 개발하여, AI 협업의 연속성과 프로젝트 지식의 지속적 축적을 지원하는 것을 목표로 한다.

### 프로젝트 소개 (300자 이내, 현재 약 270자)

Mycelium은 AI 개발 과정에서 생성되는 컨텍스트를 자동으로 수집(Capture)하고, 폴더 기반 계층 구조와 태그로 조직화(Organize)하며, 반복되는 패턴과 의사결정을 학습(Learn)하여 이후 AI 작업에 다시 활용(Reuse)하는 Context Lifecycle 플랫폼이다. 검증을 마친 실행 계층(Relay)을 통해 여러 AI 에이전트 간 컨텍스트를 이어주고, 프로젝트가 진행될수록 AI가 프로젝트의 지식과 컨벤션을 함께 학습하는 지속 가능한 AI 협업 환경을 제공한다.

### 기대 효과 (500자 이내, 현재 약 400자)

AI 코딩 에이전트를 사용하는 개발자는 모델 전환이나 사용량 제한으로 작업이 중단되거나 동일한 맥락을 반복 설명해야 하는 부담을 줄일 수 있고, 시간이 지난 뒤에도 프로젝트의 의사결정과 개발 맥락을 쉽게 복원할 수 있다. 새로운 팀원이 합류할 때도 축적된 프로젝트 컨텍스트를 통해 온보딩 비용을 크게 낮출 수 있다. 또한 축적된 컨텍스트에서 반복되는 설계 패턴과 컨벤션을 자동으로 추출해 AI의 메모리에 반영함으로써, 프로젝트가 진행될수록 AI가 프로젝트를 더 잘 이해하는 자기개선(Self-improving) 구조를 제공한다. 특정 벤더에 종속되지 않는 오픈소스로 공개되어 Claude Code, Codex 등 다양한 에이전트 환경에서 활용될 수 있으며, AI 협업의 연속성과 생산성을 높이는 공통 인프라로 발전할 수 있다.
